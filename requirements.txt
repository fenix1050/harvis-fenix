# HARVIS — Windows only (win32 APIs, WASAPI audio, UIA)
claude-agent-sdk
openai
faster-whisper
edge-tts
pygame
sounddevice
numpy
pynput
pyperclip
pywebview
pywinauto
pywin32
PyYAML
pycaw          # medidor de audio: verificar que la música REALMENTE suena
aec-audio-processing   # cancelación de eco WebRTC: el mic no escucha la música/TTS
PyAudioWPatch          # captura loopback WASAPI (lo que suena) para el AEC
psutil                 # identificar la ventana APP de música por proceso

# GPU (opcional, recomendado): DLLs de CUDA para faster-whisper.
# Sin esto cae solo a CPU con el modelo "medium".
nvidia-cublas-cu12; sys_platform == "win32"
nvidia-cudnn-cu12; sys_platform == "win32"
